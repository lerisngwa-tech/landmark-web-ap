version: '3.8'
services:
  app:
    build: .
    ports:
      - '8080:5000'
    environment:
      - MONGO_URI=mongodb://mongo:27017/landmark
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
